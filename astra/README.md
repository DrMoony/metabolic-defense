# ASTRA · Metabolic Defense

학회 부스용 마우스·절대좌표 라이트건 디펜스 게임. `beta/`의 CKLM 컨셉과 밸런스 데이터를 참고하고, 화면 구조·전투 상태 관리·3D 모델·렌더링은 새로 작성했습니다. 변경 파일은 모두 `astra/`에 있습니다.

## 실행

저장소 루트에서:

```sh
python3 -m http.server 8765
```

`http://localhost:8765/astra/` 접속. npm 설치, 번들러, CDN, 외부 폰트가 필요하지 않습니다. WebGL2 지원 브라우저가 필요합니다. `file://`로 열면 모듈·문제은행 로딩이 제한되므로 HTTP 서버를 사용하세요.

시작 화면에서 한국어/English와 EASY/NORMAL/HARD를 선택하고 **방어 시작하기 → 방어선 진입**을 누릅니다. 진입 버튼은 전체화면도 요청합니다. 지원하지 않으면 창 모드에서 진행합니다.

- 화면을 조준하고 좌클릭. 누르고 있으면 현재 무기 속도로 연사합니다.
- EASY는 무제한 탄약. NORMAL/HARD는 탄약 소진 시 자동 재장전합니다. 화면의 **재장전** 버튼으로 미리 장전할 수도 있습니다.
- **무기 교체** 버튼 또는 우클릭으로 획득한 무기를 순환합니다.
- 퀴즈는 **보기 선택 → 정답 제출**의 두 단계입니다. 제한 시간은 18초. 시간이 끝나면 선택한 답을 제출하며, 선택하지 않았으면 오답입니다.
- 결과는 4초간 표시하고 전투로 복귀합니다. **계속하기** 버튼으로 먼저 복귀할 수 있습니다.
- 화면 위의 일시정지·재개·음향·전체화면 버튼을 사용합니다. 창 포커스를 잃거나 탭을 숨기면 전투와 퀴즈를 일시정지합니다.
- 결과 화면에서 다시 도전하면 언어·난이도 선택으로 돌아갑니다. 키보드는 필수가 아닙니다.

## 3D 설계

배경 이미지나 기존 스프라이트를 사용하지 않습니다. Three.js r170을 `../vendor/three.module.js`에서 직접 가져옵니다.

- 혈관 회랑: 실제 높이를 가진 조직 덩어리, 10겹의 관형 아치, 굽은 혈관, 세 갈래 바닥 진입로, 전방 방어선과 3개 코어.
- 간: 두 개의 비대칭 엽, 발광 혈관과 정화 허브. 섬유화에 따라 색이 탁해집니다.
- 췌장: 다엽 조직과 회전하는 인슐린 포신. 인슐린 구체가 표적까지 실제 공간을 이동합니다.
- 적: 캔디·감자튀김·버거·피자·아이스크림·날개 도넛·치킨윙과 전용 보스. 장난스러운 얼굴과 움직이는 팔다리를 메시로 구성했습니다.
- 원경에서 근경으로 실제 월드 좌표를 이동하므로 원근 투영으로 크기가 커집니다. 거리별 따뜻한 조명·청록 조명, 지수 안개가 접근을 강조합니다.
- 방향광의 PCF 소프트 그림자, 금속성/거칠기 기반 하이라이트, 발광 재질, 부유 입자, 입체 파편과 지면 정화 고리.
- 깊이 텍스처 후처리로 먼 부분에 약한 흐림을 주고 밝은 부분을 확산합니다. 실제 초점 시뮬레이션이나 거울 반사 대신 원경 가독성과 재질 하이라이트에 초점을 맞췄습니다.
- 대기 화면은 간을 바라보는 카메라, 전투는 고정 방어 시점. 미세한 움직임, 총기 반동, 격파 순간 정지, 보스 슬로모를 사용합니다.
- CSS의 고정 16:9 스테이지와 동일한 캔버스 경계를 사용해 레터박스 상태에서도 절대좌표 사격을 맞춥니다.

## beta에서 참고한 수치와 변경점

`beta/main.js`, `beta/index.html`, `HANDOFF.md`, `tools/prompts.py`를 참고했습니다. 기존 로직이나 모델 코드를 복사하지 않았습니다.

| 항목 | 참고 기준 / ASTRA 적용 |
| --- | --- |
| 웨이브 | beta 35·55·65초 → ASTRA 40·60·70초. 빠른 정답 플레이도 약 3분이 되도록 각 5초 추가 |
| 보스 등장 | beta 26·42·48초 → ASTRA 31·47·53초. 등장 이후 일반 적 추가 소환 중단 |
| 보스 체력 | 시럽통 16 / 암세포 26 / 플라크 34, 난이도와 무관하게 유지 |
| 보스 특성 | 시럽통 밸브 2배 피해 / 암세포 3조각 분열 / 플라크 근거리 가속 돌진 |
| 난이도 | EASY/NORMAL/HARD: 속도 .75/.88/1, 피해 .5/.72/1, 소환 간격 1.5/1.25/1, 일반 적 HP .7/.85/1 |
| 무기 | 새총부터 레이저까지 12단. 기본 피해·연사·탄창·재장전 수치 참고. 샷건 산탄, 매그넘 관통, 기관총 3점사, 바주카 광역, 유도탄 추적 구현 |
| 무기 승급 | 퀴즈 정답 +1단, 점수 18,000마다 +1단 |
| 간 | 섬유화 25/50/75 단계, 파동 주기 9.5/11.5/13.5/17초, 반경 25 |
| 췌장 | 기능 단계에 따라 사격 효율 100/70/45/20%. 기능 5 이하에서 당류 적에 12초 누적 노출되면 그 판 영구 부전 |
| 정답 보상 | 1,500점 + 시간 보너스 최대 500, 코어 +8, 간 부담 -25, 혈당 -20, 부전 전 췌장 +30 |
| 격파 | 일반 45ms / 대형 75ms / 보스 130ms 순간 정지, 보스 0.3배속 0.55초 |

적이 방어선을 넘으면 코어 생명을 차감합니다. 간은 도달 직전의 일반 적을 약화하고 정화 파동으로 지원합니다. 혈당이 높아지면 간 부담과 췌장 소모가 커집니다. 모든 장기 수치는 게임의 서사를 위한 추상화입니다.

**세 보스를 모두 처치해야 승리**합니다. 제한 시간이 지났다고 살아 있는 보스를 삭제하지 않습니다. 보스를 놓친 웨이브는 남은 적 정리 후 실패로 끝납니다. 암세포 분열 조각도 정리해야 다음 웨이브로 이동합니다.

beta의 배치 편집기·지방 덫 구출·약물 드롭·일부 일반 적은 이 버전에 포함하지 않았습니다. 보스 보상은 이름 없는 정화 지원과 장기 회복, 일반 적 보상은 소량의 즉시 회복으로 구성했습니다. 무기 모델은 절차적으로 조립하고, 화면 레이캐스트와 공간 투사체를 별도로 관리합니다.

## 문제은행 규칙

새 문제나 비상 대체 문제를 만들지 않습니다. 아래 파일을 그대로 fetch합니다.

- `../assets/quiz_aasld_ko.json`, `../assets/quiz_aasld_en.json`
- `../assets/quiz_obesity_ko.json`, `../assets/quiz_obesity_en.json`

기본은 MASLD 70 : Clinical Obesity 30, `drug: true` 문항은 제외합니다. 시작 화면의 **운영자 설정**에서만 약물 문항을 포함할 수 있습니다. 어드민은 부스 운영 편의용 패널이며 인증 기능은 아닙니다.

설정 가능한 비율은 100:0, 70:30, 50:50, 30:70, 0:100입니다. 10문 단위 세트 일정을 Fisher–Yates로 섞어 정확한 비율을 유지합니다. 한 판은 보통 7문이므로 개별 판의 비율에는 표본 차이가 있습니다. 지정 세트 안에서 최근 출제를 피한 후 선택 난이도를 우선합니다. 풀이 소진되면 아직 이번 판에서 쓰지 않은 문제, 마지막으로 가장 오래전에 출제한 문제 순서로 완화합니다. 질문 선택과 보기 순서 모두 Fisher–Yates를 사용합니다.

최근 24문항을 `세트:원본배열인덱스`로 저장하므로 언어를 바꿔도 같은 문항의 반복을 피합니다. 원본 문제은행의 순서를 재배열한 경우에는 기존 이력이 다른 문항을 가리킬 수 있습니다.

localStorage는 전부 `astra_` 프리픽스입니다: `astra_mix`, `astra_drug`, `astra_recent`, `astra_muted`, `astra_best`. 저장 접근이 차단되어도 플레이는 가능합니다. 이때 이력과 설정의 다음 페이지 방문까지 보존은 보장하지 않습니다. 양쪽 문제은행이 모두 정상 로드될 때까지 시작을 막으며, 로딩 실패 시 재시도 버튼을 표시합니다. 언어 전환 시 오래된 로딩 응답이 최신 선택을 덮지 않도록 요청 세대를 확인합니다.

## 파일과 디버그

- `index.html`, `style.css`: 16:9 스테이지, 이중 언어 UI, 마우스 전용 조작.
- `main.js`: 전투·웨이브·퀴즈 상태, 입력, HUD, 합성 오디오.
- `world.js`: 실제 3D 모델, 조명, 그림자, 후처리, 레이캐스트, 파티클.
- `quiz.js`: 공유 문제은행과 설정·최근 이력.
- `tests/verify.mjs`: 외부 패키지 없는 로직 검증.

진입 스크립트는 `main.js?v=a3`입니다. 배포 수정 시 이 버전을 올리세요. 모듈별 캐시도 함께 갱신하도록 import URL에 같은 버전을 사용합니다.

브라우저 콘솔:

```js
ASTRA.setManual(true);       // requestAnimationFrame의 자동 진행 중지
ASTRA.start();               // 문제은행 로드 완료 후 호출
ASTRA.step(10);              // 1/60초 하위 스텝으로 10초 진행
ASTRA.state;                // 상태, 체력, 점수, 웨이브, 퀴즈 등
const enemy = ASTRA.spawn('burger', { lane: 0, progress: 0.6 });
ASTRA.step(1 / 60);
const p = ASTRA.project(enemy);
ASTRA.shot(p.x, p.y);        // 실제 화면 좌표로 사격
ASTRA.setManual(false);      // 자동 진행 복원
```

추가 핸들: `enemies`, `world`, `bank`, `WEAPONS`, `TYPES`, `WAVES`, `reload`, `swap`, `pause`, `openQuiz`, `answerQuiz`, `continueQuiz`.

## 검증 결과와 제한

```sh
node --experimental-vm-modules astra/tests/verify.mjs
```

2026-09-06 실행 결과:

- 공유 한국어/영어 JSON 로드, 기본 약물 제외, 어드민 포함, 모든 혼합 비율, 최근 이력, 저장 키 격리 통과.
- 실제 Three.js 메시·카메라·레이캐스트로 일반 사격 및 시럽통 약점 2배 피해 검증 통과.
- 재장전, EASY 무한 탄약, 퀴즈 승급, 무기 교체, 일시정지, 퀴즈 시간 초과 검증 통과.
- 레이캐스트 사격을 반복하는 NORMAL 자동 플레이로 세 웨이브·세 보스 승리 검증 통과. 빠른 퀴즈 응답에서 약 184초 기록(적/문항 무작위성으로 변동).
- 보스 누락 시 승리 방지와 췌장부전의 영구 유지 검증 통과.
- JS 문법 검사 통과.

검증기는 DOM과 WebGLRenderer를 대체하고 **실제 Three.js 기하와 레이캐스트 및 실제 게임 로직**을 실행합니다. GPU 셰이더 컴파일, 실제 브라우저 화면 배치, 오디오·전체화면, 실제 라이트건 장치 입력을 검증한 것은 아닙니다.

이 작업 환경은 `python3 -m http.server 8765`의 소켓 바인딩을 `Operation not permitted`로 차단했고 Chrome 헤드리스 실행도 중단됐습니다. 따라서 **요구된 localhost 브라우저 로드·콘솔 무오류·시각 검증은 미완료**입니다. 실행 권한이 있는 환경에서 위 서버 주소를 열고 실제 화면과 한 웨이브를 확인해야 합니다.

또한 `.git/index.lock` 쓰기가 `Operation not permitted`로 차단되어 커밋을 만들지 못했습니다. 사용할 한국어 커밋 메시지: `아스트라 3D 메타볼릭 디펜스 게임 신규 구현`.
